# Paste blog content here

Drop ONE file per post in this folder. I'll read every file here and rebuild the
post pages (clean, with Kotlin syntax highlighting). Plain text or markdown both fine.

## Filename = the post's slug (so links keep working):
- jetpack-compose-best-practices
- google-io-extended-2025-compose-multiplatform-to-city
- protobuf-generate-kotlin-code
- graphite-improve-dev-velocity-using-git
- android-adb-shortcuts-for-productivity
- encryption-google-tink-android
- jetpack-compose-security-disable-screenshots-recordings
- a11y-weather-app-android
- accessibility-list-item-android-jetpack-compose

e.g. `jetpack-compose-best-practices.md`

## Format (loose is fine):
- First line: the title (or a `# Title` heading).
- Then the body. Put code inside triple-backtick fences and I'll highlight it, e.g.

    ```kotlin
    Text(text = "Hello", modifier = Modifier.padding(16.dp).background(Color.Green))
    ```

- Headings, lists, links, images all fine. I'll handle styling, no em dashes, etc.
- You don't need every post at once. Add files as you go and tell me which are ready.
